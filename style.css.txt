/* Variáveis de cores (Tema Escuro + Verde) */
:root {
    --fundo-principal: #0F1714; /* Preto profundo esverdeado */
    --fundo-secundario: #1A2622; /* Cinza escuro esverdeado */
    --texto-principal: #E0E0E0;
    --texto-secundario: #A0AEC0;
    --cor-destaque: #00D26A; /* Verde vibrante */
    --cor-destaque-hover: #00B359;
}

/* Reset básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: var(--fundo-principal);
    color: var(--texto-principal);
    line-height: 1.6;
}

.container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Cabeçalho */
header {
    background-color: var(--fundo-secundario);
    padding: 60px 0 40px 0;
    text-align: center;
    border-bottom: 3px solid var(--cor-destaque);
}

header h1 {
    font-size: 2.5rem;
    color: var(--cor-destaque);
    margin-bottom: 10px;
}

header h2 {
    font-size: 1.2rem;
    color: var(--texto-secundario);
    font-weight: 400;
    margin-bottom: 25px;
}

nav ul {
    list-style: none;
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

nav a {
    color: var(--texto-principal);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s ease;
}

nav a:hover {
    color: var(--cor-destaque);
}

/* Seções Comuns */
.secao {
    padding: 60px 0;
    border-bottom: 1px solid #2A3B35;
}

.secao h3 {
    font-size: 1.8rem;
    color: var(--cor-destaque);
    margin-bottom: 25px;
}

.mt-2 { margin-top: 40px; }

/* Botões */
.botao, .botao-outline {
    display: inline-block;
    padding: 10px 20px;
    border-radius: 5px;
    text-decoration: none;
    font-weight: bold;
    transition: all 0.3s ease;
    margin-top: 15px;
}

.botao {
    background-color: var(--cor-destaque);
    color: #000;
}

.botao:hover {
    background-color: var(--cor-destaque-hover);
}

.botao-outline {
    border: 2px solid var(--cor-destaque);
    color: var(--cor-destaque);
    margin-right: 10px;
}

.botao-outline:hover {
    background-color: var(--cor-destaque);
    color: #000;
}

/* Cards (Habilidades e Projetos) */
.grid-habilidades {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
}

.card, .card-projeto {
    background-color: var(--fundo-secundario);
    padding: 25px;
    border-radius: 8px;
    border-left: 4px solid var(--cor-destaque);
}

.card h4, .card-projeto h4 {
    margin-bottom: 10px;
    color: #FFF;
}

.lista-soft-skills li, .card-projeto ul li, #formacao ul li {
    margin-bottom: 10px;
    margin-left: 20px;
}

.link-destaque {
    color: var(--cor-destaque);
    text-decoration: none;
    font-weight: bold;
    display: inline-block;
    margin-top: 15px;
}

/* Rodapé */
footer {
    text-align: center;
    padding: 30px;
    color: var(--texto-secundario);
    font-size: 0.9rem;
}